# See https://docs.karakeep.app/configuration for more information
DATA_DIR=<path>
NEXTAUTH_SECRET=<secret>

# SQLite remains the default. Use postgres only after the Postgres schema and
# migrations are available.
DB_DRIVER=sqlite
# DATABASE_URL=postgres://karakeep:<password>@karakeep-rw.apps.svc.cluster.local:5432/karakeep
